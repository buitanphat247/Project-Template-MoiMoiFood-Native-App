import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: { width: '100%' },
  label: { color: '#6B7280', marginBottom: 8, fontSize: 12 },
});

export default styles;


